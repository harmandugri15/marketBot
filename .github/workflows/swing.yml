name: EOD Swing Engine

on:
  schedule:
    # Runs exactly at 3:15 PM IST (09:45 UTC) every Monday-Friday
    - cron: '45 9 * * 1-5'
  workflow_dispatch: 

jobs:
  run-swing:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy requests pytz growwapi

    - name: Run Swing Bot
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: python swing_bot.py